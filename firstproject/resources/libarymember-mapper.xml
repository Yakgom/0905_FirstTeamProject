<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<properties>

	<comment>
	libarymember-mapper
	</comment>
	
	<entry key="memberInsert">
		INSERT
		  INTO
		       TB_LIBRARY_MEMBER(
		       					 MEMBER_NO
		       				   , MEMBER_NAME
		       				   , MEMBER_PHONE)
		VALUES
		       (
		       SEQ_LIBRARY_MEMBER_NO.NEXTVAL
		     , ?
		     , ?
		       )	  
	</entry>
	
	<entry key="memberUpdate">
		UPDATE
		       TB_LIBRARY_MEMBER
		   SET
		       MEMBER_PHONE = (SELECT
		                              MEMBER_PHONE
		                         FROM
		                              TB_LIBRARY_MEMBER
		                        WHERE
		                              MEMBER_PHONE = ?)
	</entry>

   
</properties>


